:15277 {
  encode zstd gzip

  # Use Caddy's internal CA for TLS (self-signed). Browsers will show a warning
  # unless you install the CA. For trusted certs without opening 80/443, use
  # DNS-01 (requires provider plugin) or mount your own certs and use 'tls' below.
  tls internal

  # If you have your own certs, comment 'tls internal' above and use:
  # tls /certs/fullchain.pem /certs/privkey.pem

  reverse_proxy control-api:15277
}
